## Descripción:  
Crea una función `createCharacter` en TypeScript que:

- Reciba:
  - `name` (obligatorio)
  - `ability` (opcional)
  - `weapon` (por defecto: `"espada"`)

- Devuelva un mensaje:
  - Con habilidad → incluye nombre, habilidad y arma
  - Sin habilidad → indica que no tiene habilidades especiales

- Prueba la función con:
  - Un personaje completo
  - Uno sin habilidad
  - Uno con arma por defecto
  
## **Solución:**
[**Pulsa aquí**](index.ts)